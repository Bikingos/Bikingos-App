<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary_transparente</item>
        <item name="colorPrimaryDark">@color/primary_black_dark</item>
        <!--<item name="colorAccent">@color/accent_red</item>-->
        <item name="android:background">@color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorControlActivated">@color/accent_red</item>
        <item name="colorControlHighlight">@color/accent_red</item>
        <item name="android:windowTranslucentStatus">true</item>

    </style>

</resources>
